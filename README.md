# Hero's Junk Removal

Veteran-owned junk removal website for Dallas-Fort Worth. Owned by Todd, run with his son Nash.

Static site — plain HTML/CSS/JS, no build step. `index.html` is the home page.

## Deploy (Cloudflare Pages)
- Framework preset: **None**
- Build command: **(leave blank)**
- Build output directory: **/** (root)

Every push to `main` auto-deploys.

Built by Wing Digital.
